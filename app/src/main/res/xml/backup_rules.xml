<?xml version="1.0" encoding="utf-8"?>
<!--
  Used on API <= 30 (android:fullBackupContent). Backup is already disabled via
  android:allowBackup="false", but we additionally exclude every path so that even
  a manifest-merge override cannot cause the encrypted vault or key material to be
  copied off-device.
-->
<full-backup-content>
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="external" path="." />
    <exclude domain="root" path="." />
</full-backup-content>
